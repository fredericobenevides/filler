{:paths ["src"]
 :tasks {:requires ([filler.init :as init]
                    [filler.fillers :as fillers])
         init (init/init)
         clear (init/clear)
         fillers (fillers/list)
         fill-with (exec 'filler.fillers/run-filler)}}
